<openejb>
<!-- There is a resource leak somewhere in the Framework's JUnit code. This
     container works around it.  The key here is the non-strict pooling -->
<Container id="My Stateless Container" type="STATELESS">
    TimeOut 0
    PoolSize 20
    StrictPooling true
</Container>
<Resource id="@jndi.data.source@"  type="DataSource">
    JdbcDriver = @test.jdbc.driver@
    JdbcUrl = @test.jdbc.url@
    Username = @test.jdbc.username@
    Password = @test.jdbc.password@
    DefaultAutoCommit = false
</Resource>
</openejb>
